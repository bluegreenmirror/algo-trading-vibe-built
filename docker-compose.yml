services:
  app:
    environment:
      - PYTHONUNBUFFERED=1
    build:
      context: .
      dockerfile: Dockerfile 
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - .:/app
